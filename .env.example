# Backend API base URL
VITE_API_URL=http://localhost:8080

# Set to true to use mock data instead of real API calls (see app/mocks/)
VITE_USE_MOCKS=false

# Set to true only when the backend watering events endpoint is available (pending IoT)
VITE_WATERING_EVENTS_ENABLED=false
